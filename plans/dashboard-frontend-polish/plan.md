# Dashboard Frontend Polish Plan

1. Replace random image references with `public/assets/skill-map-teamwork-hero.png`.
2. Add a small reusable tooltip pattern around icon-only buttons.
3. Normalize text/icon labels in header, nav, profile actions, and destructive states.
4. Consolidate dark CSS overrides for cards, buttons, filters, form controls, empty/loading/error panels, modal/select surfaces, and responsive layout.
5. Verify with tests, production build, and browser smoke.
